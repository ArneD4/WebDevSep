"use strict";
for (let i=0; i<numbers.length;i++){
    let a = numbers[i];
for(let j = 0; j<numbers.length;j++){
    let b = numbers[j]
for(let k = 0;k<numbers.length;k++){
    let c = numbers[k]
    if( a + b + c === 2020){
        let result = a*b*c
        console.log(a)
        console.log(b)
        console.log(c)
        console.log(result)
    }
}
}}
